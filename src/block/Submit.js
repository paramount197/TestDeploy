import React from "react";
import '../styles/submit.css';

function Submit(sub) {
    return (
        <input
            type="submit"
            value="Submit"
            className="submit"
        />
    );
}

export default Submit;